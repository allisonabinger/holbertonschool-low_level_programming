#include "main.h"
/**
 * _memset - fills the first n bytes of memory area pointed to by s
 * with the constant byte b
 * @s: pointer to string
 * @b: byte used to fill
 * @n: amount of bytes of s to fill
 * Return: pointer to the memory area s
 */
char *_memset(char *s, char b, unsigned int n)
{
	int i;

	for (i = 0, i < n; i++)
	{
		buffer[i] = b;
	}
	return (s)
}
